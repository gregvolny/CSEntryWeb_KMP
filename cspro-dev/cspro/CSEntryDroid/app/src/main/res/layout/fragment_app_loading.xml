<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="10dp"
    tools:context="gov.census.cspro.csentry.AppLoadingFragment">

    <ProgressBar
        android:id="@+id/progressbar_app_loading"
        style="?android:attr/progressBarStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <TextView
        android:id="@+id/text_app_loading_message"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:layout_marginStart="20dp"
        android:layout_marginLeft="20dp"
        android:layout_centerVertical="true"
        android:layout_alignBottom="@+id/progressbar_app_loading"
        android:layout_alignTop="@+id/progressbar_app_loading"
        android:layout_toEndOf="@id/progressbar_app_loading"
        android:layout_toRightOf="@id/progressbar_app_loading"
        android:text="@string/starting_application" />

</RelativeLayout>
